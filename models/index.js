require("../config/db.connection");

module.exports = {
    User: require('./User'),
    Posts: require('./Posts')
}

require("../config/db.connection");

module.exports = {
    Products: require('./Product'),
    Review: require('./Review'),
    User: require('./User')
}